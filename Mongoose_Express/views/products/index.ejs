<!DOCTYPE html>
<html lang="en">
<head>
    <title>Products</title>
</head>
<body>
    <h1><%= category %> Products</h1>
    <ul>
        <% for (let product of products) { %>
            <a href="/products/<%= product._id %>"><li><%= product.name %></li></a>
        <% } %>
    </ul>
    <a href="/products/new">New Product</a>
    <% if (category !== 'All') { %>
        <a href="/products">All Products</a>
    <% } %>
</body>
</html>